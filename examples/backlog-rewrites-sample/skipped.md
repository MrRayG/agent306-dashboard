# Skipped items

Items in the input `backlog.json` that this assistant did NOT draft.
Memory-origin items are intentionally skipped (operator-only promotion).

| id | bucket | reason |
| --- | --- | --- |
| `memory:mem_001` | `promote_later_memory_origin` | bucket not in --buckets (promote_later_memory_origin) |
| `memory:mem_002` | `promote_later_memory_origin` | bucket not in --buckets (promote_later_memory_origin) |
